# narrative-forge
Narrative Forge - The 11-domain interactive truth engine exposing legacy media, political, bureaucratic, legal, financial, cultural, and health systems tactics through thought bridges, graphs, flowcharts, memes, and public data synthesis. Built with PHP/Composer backend + modern JS frontend.

**MAX SUSTAIN Visuals/4D/Sonif/Liquid/Particles/High-Contrast + Archetypes 11 Workgroup completed (pretrained golden neural-map-holo.html + previews).** Enhanced high-contrast ZDF reclaim + 4D layers/sonif bidirectional + archetype motif polish (nimrod/ZDF glitch haman heavy goliath flow jezebel) + Liquid fresnel synergy + particles trails boost on ZDF + demo seqs. Exact Liquid SVG feTurbulence baseFrequency="0.015" numOctaves="2" seed="42". 4D scrub zdf-lie t~-6.8 + glitch sonif + reclaim hottest. FPS 57-61 / motif coverage 11/11+ZDF reported. Background no-idle. Parity golden holo<->previews. Credits + delegated todos in IMPLEMENTATION_TASKS.md. Spawned verifier 019ed0a1-42ff-7e1c-9c3a-max-sustain-visuals-verifier. MAX visuals field.

## RSS News (rss_news source) - Liveness & New News Features
- Added dedicated `rss_news` source in monitor/backfill + new lightweight `rss-monitor.yml` (cron `*/30 * * * *`).
- Discover pulls from public high-signal RSS (Reuters, AP, NYT, BBC, CNN, Guardian, WSJ, gov feeds) → ingests article HTMLs to `raw/news/rss-*-*.html` + feed XML snapshots to `raw/media/rss-*.xml` (and `raw/news/`).
- **Auto chaining for efficiency & liveness**: `ingest RSS` (monitor or rss-monitor) → `process-data.yml` (text extract + entity/signals for news items) → `analyze-data.yml` (new news features: repeated phrases/coordination detection, framing density, timelines, tactic scores, entity graphs from current events) → `build-politician-profiles.yml` if entities detected.
- **Liveness benefits**: 30min cadence catches breaking news/gov releases fast (last-hour items in health checks). Low-overhead (small max_new, parallel futures for feeds in discover_rss_news_new, short windows). Complements daily broad monitor + weekly backfill (rss_news supports --backfill for historical news story depth).
- **Health**: Use `python scripts/check_r2_health.py --rss --liveness-hours 1` (counts in raw/news/ + raw/media/rss-*, samples, last-hour live items, R2 path hits, NOTIFY guidance). Cloudflare R2 UI cross-check under `raw/news/` and `raw/media/`.
- **Dispatch examples**:
  - Lightweight live: `gh workflow run rss-monitor.yml` (or auto every 30min).
  - Full with RSS: `gh workflow run monitor-ingest.yml -f sources=rss_news,gdelt,... -f auto_process=true`.
  - Historical RSS: `gh workflow run backfill.yml -f sources=rss_news -f backfill-level=deep`.
  - Manual specific: after ingest, `gh workflow run process-data.yml -f arena=news -f raw_key=raw/news/rss-...` then analyze with the processed_key.
- Sources list (in discover): reuters, ap-*, nyt-*, wsj, bbc, cnn, guardian + gov (whitehouse, justice, gao). Extend in monitor_and_ingest.py.
- See DATA_PROCESSING.md, DATA_SOURCES_TESTED.md, workflows/*.yml, scripts/monitor_and_ingest.py (rss special casing + futures concurrency), scripts/check_r2_health.py.
- Keeps everything efficient and live: targeted RSS avoids full scan cost, futures parallelize, auto-chain only on new, health verifies paths/live state.

## Holographic Neural Map + R3F Migration (Docs/Delegation focus)
- Core: neural-map-holo.html (full self-contained vanilla Three prototype) + neural-map-holo-guts.md (extracted + now expanded).
- **guts.md updated** (per workgroup items 10/14/15): 50+ granular delegable todos with [OPEN: ... / Visuals/Perf/Shader ...] team markers; ... (see file). **Perf suite complete** (Benchmarking subagent): 100/500/1k/10k benchmarks (FPS/mem/draws via three.info + Spector), WebGPU fallback+basic impl, CI, vanilla vs R3F compare; reporting integrated (PERF_REPORT.md + updates to RUN_NOTES/IMPLEMENTATION_TASKS/README/guts). Harness + playwright + holo enhanced. Initial benchmarks run. **WebGPU and advanced fallbacks completed by subagent 019ecd00-0572-7501-a19e-08af9f545340 (verifier/enhancer & integrator + ZDF enhancements for "Jagd auf Migranten" fabrication lie explicit demo in paths + bursts + 4D). New spawn ID 019ecd50-0572-7501-a19e-08af9f545340-verifier. Field hot for continuous orchestrator.**
**Sonification and 2D alt viz completed by background subagent 019eccff-2b6e-7933-8f95-32bdeb5ea507 (Sonification and 2D alt viz completed, 639.9s/117 calls).** Verified in golden (full WebAudio sonif hot pulses + 11 arch motifs incl ZDF glitch square for fabrication lie + 4D/ZDF mod + sonif viz HUDs + advanced 2D fallback orbs/embers full + export sonif bundles) + narrative-forge parity + mappers ties + docs. Enhanced for ZDF/Elon (deeper glitch/pitch on ZDF LIE preset, lie alarm nimrod fallback, sonif state in exports for suit, lite/reduced/PWA notes, cross parity). Wiring/harness green. New spawn 019eccff-2b6e-7933-8f95-32bdeb5ea507-verifier. Rec to continuous orchestrator: keep field hot (ZDF/Elon pinnacle glitch sonif + 4D/fallback evidence).
- **FINAL PROD SMOKE + ROADMAP (guts + IMPL + this README updated)**: Prod smoke green (see full in guts.md + narrative-forge/deploy.sh + README). End product achieved: max subagents/fields (~38+ across R2 live/ZDF/ particles/perf/testing/archetypes/deploy/analytics/WebGPU/golden, all viz/export/4D/sonif/labels/particles/HUD), live R2 actuals flowing, ZDF as pinnacle Elon forensics tool, prod ready paths for both (deprecated Next R3F, golden static demo), 20+ granular tasks delegable remaining. Prod build/test /holo full ZDF/JARVIS/poll/3D/sonif/analytics green; golden static http-server/gh-pages/CF for neural-map-holo.html+preview-4+data/+sw/manifest + R2 poll/MASTER/ZDF visible confirmed. All recent polled (new WebGPU test 019ecd35-b7a8-78a1-88eb-0c0160ffca2d + golden R2+ZDF verify 019ecd391778 + recent success verifies 019ecd3c-74bb-7be0-bc5a-e9145388af0f, 019ecd3c-74bb-7be0-bc5a-e92049861b23, 019ecd3a-3ca3-71b3-a10b-2eb26d949380 + scheduler spawns 019ecd3e83a6 + gap 019ecd2fc2a4). 1 gap filler (019ecd2fc2a4) + final spawns. Max subagents/fields fully sustained (~38+), end-product path locked in, ZDF as Elon pinnacle tool. Report: "Prod smoke green. Roadmap: end-product path clear, max sustained, Elon ZDF tool pinnacle. Poll final IDs." (follow-ups: 019ecd21-df1d-72c0-99fc-ca1bbb278a83 + 019ecd1e-b3de-7551-95e7-7510551b3231 + 019ecd1e-b3de-7551-95e7-74ea0661d5e3 + 019ecd391778 + all priors). Quick fidelity/serve verified. Max subagents/fields fully sustained (~38+), end-product path locked in, ZDF as Elon pinnacle tool.
**Deployment CI/CD for end product subagent completed by 019ecd00-0572-7501-a19e-084c6fa8237e (background 813.8s/166 calls).** Explicit credit + \"Deployment CI/CD for end product subagent completed\" added to this README + IMPLEMENTATION_TASKS.md (narrative-forge), neural-map-holo-guts.md, deploy.sh (narrative-forge e2e). Verified: golden static deploy recipes (npx http-server / gh-pages / CF Pages / vercel static for neural-map-holo.html + preview-4 + data/ + sw.js + manifest.json; no-build; PWA/offline); cross with narrative-forge vercel prod. Enhanced: ZDF as Elon pinnacle in golden static docs (4D LIE scrub + glitch sonif + labels + particles + reclaim + poll/MASTER visible; deep links; export; prod links referenced). CI parity notes (ZDF goldens/snapshots via CI workflows + prod smoke). R2 proxy for live in static + ZDF usage. Cross consistency (golden self-contained PWA vs narrative-forge vercel/R2 proxy; ZDF pinnacle both; no drift in deploy recipes). New spawn 019ecd53-0572-7501-a19e-084c6fa8237e-verifier. Rec to continuous orchestrator: keep deployment CI/CD + ZDF/Elon field hot/no-idle as pinnacle suit tool (full prod deploy + CI locked). MAX SUSTAIN.
**Migration to prod R3F/Next subagent completed:** 019eccfe-e6f1-7db1-997c-e5d93c657ec9 (795.3s/141 calls) completed. Verifier/enhancer post-run: full migration to narrative-forge (prod R3F/Next) with exact parity to golden neural-map-holo.html (immutable self-contained vanilla review artifact). ZDF/Elon 4D/glitch sonif/export/sink/R2 deltas/WebGPU/PWA/deep links full in R3F. R2_PUBLIC_BASE + deploy recipes verified. New spawn 019ecd52-8ec2-7840-a665-16d9-r3f-prod-verifier. "Migration to prod R3F/Next subagent completed". Field hot.
- **DATA_*.md all enhanced**: "Catalog Summaries from Data Field" sections with real extracted stats (politicians-index 691 entries + schema/samples/arenas; news-synthesis ripples e.g. "humanitarian implementation hiccups" sim0.82 + outlets/graph/tactics from rss liveness subagent; profiles ~914 w/ mediaFraming; hot calc/Rule42/infer mappers; R2 provenance/liveness ties).
- Prior subagent syntheses reviewed: R2_VERIFIER_CYCLE.md (SUBAGENT#3 health/dispatches), all preview-iteration-*.html + home-*.html (UI iterations w/ liquid-glass subagent notes, archetype filters, hot formula from preview-4, live RSS). **UI polish across previews completed by 019eccfe-e6f1-7db1-997c-e5b6e9679e0a (full verify + ZDF/Elon pinnacle enhancements in previews + golden sync + credit in guts/IMPL/READMEs; new spawn 019ecd4a-8ec2-7840-a665-16c8-ui-polish-verifier). Preview-4 remains key hybrid wiring ref (holoAPI from chips/cases).**

**Archetypes Expansion (subagent 019eccfe-e6f1-7db1-997c-e5ac08e23aa5) completed**: "Archetypes expansion completed". 11 rich + ZDF/Elon deep (nimrod fabrication lie 4D timeline for suit, reclaim "Scatter Babel lie with X quote", GEZ/pharaoh/haman, etc). Verified + enhanced in golden holo (11 filters/evidence/JARVIS/tour/ZDF LIE 4D) + shared mappers/HoloMap (CASE_ZDF/r2 nodes). Added deeper 4D narrative/reclaim seq/'Elon ZDF Suit Evidence' bundles (4D JSON+PNG+prov+quotes+reclaim+edges) + motif/shader/particle + cross. New verifier spawn 019ecd52-e6f2-7db2-997d-f5ad09e34bb6. Recommendation: continuous orchestrator sustain this ZDF field hot. See guts/IMPL for details.

**Single-repo narrative-forge deployment by background subagent 019eccff-2b6e-7933-8f95-32a32c3d26f5.** Explicit credit + "Single-repo narrative-forge deployment" in this README, IMPLEMENTATION_TASKS.md, neural-map-holo-guts.md (and cross narrative-forge docs/TEST/IMPLEMENTATION/README). Verifier/enhancer confirmed full ZDF/Elon parity + cross-wiring (4D scrub/glitch sonif/export signed bundles with 4D/PNG/prov/X/reclaim/analytics sink/live deltas/WebGPU/fallback/PWA/deep links in both golden self-contained + deprecated R3F port). Golden review artifact; narrative-forge prod. Exact mappers. Fidelity roundtrip ZDF PASS. New spawn 019ecd52-8ec2-7840-a665-16e0-holo-verifier. Keep field hot; rec to continuous orchestrator (ZDF/Elon pinnacle sustain).
- Data: data/politicians-index.json, news-synthesis.json, profiles/, news-sample.json + scripts/update_politicians_neural_map.py + R2 workflows (monitor/rss).
- Deploy target: narrative-forge static golden holo — see its README + copy skeleton from guts. Deploy: static holo.html artifact on thebreakerofbabylon.com.
- Delegate: use expanded list in guts.md; open teams for parallel work. R2 live via existing crons + SWR.

Cross-ref R2_VERIFIER_CYCLE.md, preview-*.html, DATA_*, holo.html, scripts/, .github/workflows/.
